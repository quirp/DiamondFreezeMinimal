/**
 * Main entrypoint for generating a verification.sol contract.
 */

function verifierSolGenerate(){
    /**
     * //pragma import
     * //file imports
     * //insert verifier function
     * //insert both trees
     * 
     */
    //load pragma version from config file
    //l
}