/**
 * Checks freezeables are of proper type
 * - Load in AST of config.sol
 * - Assert no mappings or dynamic arrays
 * - 
 */

function validateConfigSol(freezeables,configSol){
    //assert proper types (exclude blacklisted )
    //copy values

}


module.exports = {validateConfigSol}