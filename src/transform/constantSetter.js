/**
 * We loop over all freezeables. 
 * We make edits for the constant declarations and getters
 * 
 */
 
/**
 * 
 * @param {*} freezables 
 */
function constantSetter( freezables ){
    //loop over freezeables
    //
}